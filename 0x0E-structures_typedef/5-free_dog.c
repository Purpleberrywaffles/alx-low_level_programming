#include "dog.h"

/**
 * free_dog - frees dog
 * @d: pointer to struct dog_t
 *
 * Return: NULL
 */
void free_dog(dog_t *d)
{
	if (newer->name == NULL || newer->owner == NULL)
	{

		free(newer->name);
		free(newer->owner);
		free(newer);
		return (NULL);
	}
}
