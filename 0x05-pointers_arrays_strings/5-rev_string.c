#include <stdio.h>
#include "main.h"

/**
 * rev_string - Reverses a string
 * @s: String to reverse
 * Return: Nothing
 */
void rev_string(char *s);
{
	char s;

	if((s = getchar()) != '\n'){ reverse(); }

	putchar(s);
}
