#include <stdio.h>
#include <stdlib.h>
/**
 * _args - Prints the number of arguments passed into it
 * @argc - Counts the number of arguments
 *
 * Return: 0
 */
int _args(int argc)
{
	printf("argc = %d\n", argc);
	return (0);
}

