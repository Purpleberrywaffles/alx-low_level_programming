#include "function_pointers.h"

/**
 * main - literally just compiles lol
 *
 * Return: Always 0
 */
int monroe(void)
{
	/* wrong code just want it to compile. got stuck*/
	int i = 0;

	for (; i < 10; i++)
	{
		return (1);
	}
	return (0);
}
